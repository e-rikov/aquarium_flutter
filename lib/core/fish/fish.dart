import 'dart:math';

class Fish {

    final int size = throw UnimplementedError();
    final bool isAngry = throw UnimplementedError();
    final String image = throw UnimplementedError();
    final Point<double> position = throw UnimplementedError();
    final double dx = throw UnimplementedError();
    final double dy = throw UnimplementedError();


    Function onChangeListener = throw UnimplementedError();


    bool canEat(Fish fish) => throw UnimplementedError();

    dispose() {
        throw UnimplementedError();
    }

    makeStep() {
        throw UnimplementedError();
    }

}